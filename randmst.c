#include <stdio.h>

// tuple structure
typedef struct {
	int x;
	int y;
	int z;
	int a;
} coord;

int main(int argc, char* argv[]) {
	if (argc != 5) {
		printf("Usage: randmst 0 numpoints numtrials dimension\n");
		return 1;
	}

	// TODO: Given numpoints, generate n int arrays of size n-1 (.
	// 		 

	int graph[numpoints][numpoints];

}